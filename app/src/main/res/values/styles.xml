<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionBarTheme">@style/AppTheme.AppBarOverlay</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="colorControlActivated">#ffffff</item>
        <item name="android:actionMenuTextAppearance">@style/AppTheme.TextAppearanceActionMenuItem</item>
    </style>

    <style name="AppTheme.TextAppearanceActionMenuItem" parent="TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <item name="android:textColor">#c9cacc</item>
        <item name="android:fontFamily">@font/titilliumwebregular</item>
    </style>


</resources>